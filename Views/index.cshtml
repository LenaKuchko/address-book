@{
  Layout="Shared/_Layout.cshtml";
}
<h1>Your Address Book</h1>
@if(@Model.Count == 0)
{
  <h2>There is no contacts in the AddressBook</h2>
}
else
{
  <ul>
    @foreach(var contact in @Model)
    {
      <h3><li><a href="/contacts/@contact.GetId()">@contact.GetName()</a></li></h3>
    }
  </ul>
  <!-- @foreach(var item in @Model)
  {
    <h3><ul><strong>@item.GetName()</strong></h3>
      <li>@item.GetPhoneNumber()</li>
      <li>
        <ul><strong>Address</strong>
          <li>@item.GetAddress().GetState()</li>
          <li>@item.GetAddress().GetCity()</li>
          <li>@item.GetAddress().GetStreet()</li>
        </ul>
      </li>
    </ul>
  } -->
}
<h2><a href="/contacts/new">Create new Contact</a></h2>
